---
phase: 02-agent-architecture-governance-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/proposals.ts
  - src/events/event-bus.ts
  - src/events/event-types.ts
  - src/events/index.ts
  - src/agents/types.ts
  - src/agents/base-agent.ts
  - src/agents/stubs/stub-scout.ts
  - src/agents/stubs/stub-analyzer.ts
  - src/agents/stubs/stub-treasury.ts
  - src/agents/index.ts
  - tests/unit/event-bus.test.ts
  - tests/unit/base-agent.test.ts
autonomous: true
requirements:
  - GOV-01
must_haves:
  truths:
    - "A TypedEventBus emits and receives events with compile-time type safety"
    - "BaseAgent provides keypair access, event bus connection, and lifecycle hooks for any agent role"
    - "Stub agents implement the same interfaces that real agents will use in Phases 3-5"
    - "Agent interfaces define typed contracts for discover, evaluate, and fund operations"
  artifacts:
    - path: "src/types/proposals.ts"
      provides: "Proposal, Evaluation, FundingAllocation, DecisionSummary, TransactionResult, TreasuryBalance types"
      exports: ["Proposal", "EvaluationScores", "Evaluation", "FundingAllocation", "DecisionSummary", "TransactionResult", "TreasuryBalance"]
    - path: "src/events/event-bus.ts"
      provides: "Generic TypedEventBus class wrapping Node.js EventEmitter"
      exports: ["TypedEventBus"]
    - path: "src/events/event-types.ts"
      provides: "AgentEvents type map and AgentEventBus type alias"
      exports: ["AgentEvents", "AgentEventBus", "AgentStatusEvent", "PipelineStepEvent", "PipelineDecisionEvent"]
    - path: "src/agents/types.ts"
      provides: "IScoutAgent, IAnalyzerAgent, ITreasuryAgent interfaces"
      exports: ["IScoutAgent", "IAnalyzerAgent", "ITreasuryAgent"]
    - path: "src/agents/base-agent.ts"
      provides: "Abstract BaseAgent class with keypair, bus, lifecycle"
      exports: ["BaseAgent"]
    - path: "src/agents/stubs/stub-scout.ts"
      provides: "StubScoutAgent returning mock proposals"
      exports: ["StubScoutAgent"]
    - path: "src/agents/stubs/stub-analyzer.ts"
      provides: "StubAnalyzerAgent returning mock evaluations"
      exports: ["StubAnalyzerAgent"]
    - path: "src/agents/stubs/stub-treasury.ts"
      provides: "StubTreasuryAgent returning mock transaction results"
      exports: ["StubTreasuryAgent"]
  key_links:
    - from: "src/agents/base-agent.ts"
      to: "src/events/event-types.ts"
      via: "AgentEventBus type for bus parameter"
      pattern: "AgentEventBus"
    - from: "src/agents/base-agent.ts"
      to: "src/lib/keys.ts"
      via: "getWeb3Keypair for agent keypair"
      pattern: "getWeb3Keypair"
    - from: "src/agents/stubs/stub-scout.ts"
      to: "src/agents/types.ts"
      via: "implements IScoutAgent"
      pattern: "implements IScoutAgent"
    - from: "src/agents/stubs/stub-analyzer.ts"
      to: "src/agents/types.ts"
      via: "implements IAnalyzerAgent"
      pattern: "implements IAnalyzerAgent"
    - from: "src/agents/stubs/stub-treasury.ts"
      to: "src/agents/types.ts"
      via: "implements ITreasuryAgent"
      pattern: "implements ITreasuryAgent"
---

<objective>
Create the typed coordination foundation: event bus, base agent class, agent interfaces, stub implementations, and proposal domain types.

Purpose: Establish the contracts and infrastructure that the GovernanceAgent (Plan 02) and all future specialist agents (Phases 3-5) build against. Interface-first design ensures stubs and real implementations are interchangeable.

Output: 10 source files (types, events, agents, stubs) + 2 test files, all compiling and tests passing.
</objective>

<execution_context>
@/Users/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-architecture-governance-core/02-RESEARCH.md
@.planning/phases/02-agent-architecture-governance-core/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts from Phase 1 that this plan depends on. -->

From src/types/agents.ts:
```typescript
export const AGENT_ROLES = ['scout', 'analyzer', 'treasury', 'governance'] as const;
export type AgentRole = (typeof AGENT_ROLES)[number];
export interface AgentConfig {
  role: AgentRole;
  name: string;
  description: string;
  metadataUri?: string;
}
export const AGENT_CONFIGS: Record<AgentRole, AgentConfig>;
```

From src/lib/keys.ts:
```typescript
export function getWeb3Keypair(role: AgentRole): Keypair;
export function getUmiSigner(role: AgentRole): KeypairSigner;
export function getAllWeb3Keypairs(): Record<AgentRole, Keypair>;
export type { AgentRole } from '../types/agents.js';
```

From src/lib/solana/connection.ts:
```typescript
export function getConnection(): Connection;
export function resetConnection(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type definitions, TypedEventBus, and event types</name>
  <files>src/types/proposals.ts, src/events/event-bus.ts, src/events/event-types.ts, src/events/index.ts, tests/unit/event-bus.test.ts</files>
  <behavior>
    - EventBus emit/on: emitting an event triggers the registered listener with correct args
    - EventBus once: listener fires exactly once then auto-removes
    - EventBus off: removed listener does not fire on subsequent emits
    - EventBus removeAllListeners: clears all listeners for a given event
    - EventBus type safety: AgentEventBus correctly types event names and payloads (compile-time -- verify via TypeScript compilation)
  </behavior>
  <action>
Create `src/types/proposals.ts` with all domain types matching the RESEARCH.md definitions exactly:
- `Proposal` (id, title, description, requestedAmount, teamInfo, sourceUrl?)
- `EvaluationScores` (teamQuality, technicalFeasibility, impactPotential, budgetReasonableness -- all number 1-10)
- `Evaluation` (proposalId, proposalTitle, scores, overallScore, reasoning, recommendation)
- `FundingAllocation` (proposalId, proposalTitle, action, amount?, reasoning)
- `DecisionSummary` (timestamp, summary, allocations, totalAllocated, remainingBudget)
- `TransactionResult` (success, signature?, error?)
- `TreasuryBalance` (solBalance, usdcBalance, totalValueUsd)

Create `src/events/event-bus.ts` with the `TypedEventBus<TEvents>` generic class wrapping Node.js `EventEmitter`. Methods: `emit`, `on`, `off`, `once`, `removeAllListeners`. Follow the exact pattern from RESEARCH.md Pattern 1. The class should be generic over `TEvents extends Record<string, any[]>`.

Create `src/events/event-types.ts` with:
- `AgentStatusEvent` interface (agent: AgentRole, status: string, detail?: string, timestamp: number)
- `PipelineStepEvent` interface (step: 'discover' | 'evaluate' | 'decide' | 'fund', status: 'started' | 'completed' | 'failed', detail?: Record<string, unknown>)
- `PipelineDecisionEvent` interface (summary: string, allocations: Array<{proposalTitle, action, reasoning}>)
- `AgentEvents` type mapping event names to payload tuples
- `AgentEventBus` type alias = TypedEventBus<AgentEvents>

Create `src/events/index.ts` barrel re-exporting TypedEventBus, all event types, and AgentEventBus.

Write `tests/unit/event-bus.test.ts` FIRST (RED), then implement. Test emit/on, once, off, removeAllListeners behaviors using a simple test event map.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/event-bus.test.ts --reporter=verbose</automated>
  </verify>
  <done>TypedEventBus passes all 4+ behavioral tests. All types compile. Barrel export works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BaseAgent abstract class, agent interfaces, and stub implementations</name>
  <files>src/agents/types.ts, src/agents/base-agent.ts, src/agents/stubs/stub-scout.ts, src/agents/stubs/stub-analyzer.ts, src/agents/stubs/stub-treasury.ts, src/agents/index.ts, tests/unit/base-agent.test.ts</files>
  <behavior>
    - BaseAgent concrete subclass can be instantiated with a role and bus
    - BaseAgent.publicKey returns the correct public key for the given role
    - BaseAgent.role returns the role string
    - BaseAgent.emitStatus emits an 'agent:status' event on the bus with correct payload shape
    - BaseAgent.initialize and shutdown are callable lifecycle methods
    - StubScoutAgent.discoverProposals returns an array of Proposal objects
    - StubAnalyzerAgent.evaluateProposal returns an Evaluation object with scores
    - StubTreasuryAgent.executeFunding returns a TransactionResult
    - StubTreasuryAgent.getBalance returns a TreasuryBalance
  </behavior>
  <action>
Create `src/agents/types.ts` with agent interfaces:
- `IScoutAgent` with `discoverProposals(query: string): Promise<Proposal[]>`
- `IAnalyzerAgent` with `evaluateProposal(proposal: Proposal): Promise<Evaluation>`
- `ITreasuryAgent` with `executeFunding(allocation: FundingAllocation): Promise<TransactionResult>` and `getBalance(): Promise<TreasuryBalance>`
- Import Proposal, Evaluation, FundingAllocation, TransactionResult, TreasuryBalance from `../types/proposals.js`

Create `src/agents/base-agent.ts` following RESEARCH.md Pattern 2 exactly:
- Abstract class with readonly role, keypair, protected bus
- Constructor takes AgentRole and AgentEventBus, calls getWeb3Keypair(role) for keypair
- publicKey getter returns keypair.publicKey
- Protected connection getter returns getConnection()
- Protected emitStatus method emits 'agent:status' event
- Abstract initialize() and shutdown() methods

Create `src/agents/stubs/stub-scout.ts` -- implements IScoutAgent, returns 2-3 realistic mock proposals per RESEARCH.md examples.

Create `src/agents/stubs/stub-analyzer.ts` -- implements IAnalyzerAgent, returns a mock Evaluation with hardcoded scores (team: 7, feasibility: 8, impact: 6, budget: 7, overall: 7.0) and mock reasoning text. Recommendation based on overall score >= 6 -> 'fund', else 'reject'.

Create `src/agents/stubs/stub-treasury.ts` -- implements ITreasuryAgent. executeFunding returns { success: true, signature: 'stub-tx-' + random hex }. getBalance returns { solBalance: 10, usdcBalance: 50000, totalValueUsd: 51500 }.

Create `src/agents/index.ts` barrel re-exporting BaseAgent, all interfaces, and all stubs.

Write `tests/unit/base-agent.test.ts` FIRST (RED), then implement. Create a concrete TestAgent extending BaseAgent for testing. Verify keypair loading, role, publicKey, emitStatus behavior, and that stub agents satisfy their interfaces.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/base-agent.test.ts --reporter=verbose</automated>
  </verify>
  <done>BaseAgent subclass instantiates with correct keypair and role. emitStatus emits typed events. All 3 stub agents return correctly shaped data. All tests pass. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
All Phase 2 Plan 01 artifacts verified:
- `pnpm exec vitest run tests/unit/event-bus.test.ts tests/unit/base-agent.test.ts --reporter=verbose` -- all tests green
- `pnpm exec tsc --noEmit` -- TypeScript compiles with zero errors
- Stub agents return data matching the Proposal, Evaluation, TransactionResult, TreasuryBalance interfaces
</verification>

<success_criteria>
1. TypedEventBus emit/on/off/once/removeAllListeners all work with compile-time type checking
2. BaseAgent abstract class provides keypair, bus, connection, and lifecycle hooks
3. IScoutAgent, IAnalyzerAgent, ITreasuryAgent interfaces defined with exact method signatures
4. Three stub implementations satisfy their interfaces with realistic mock data
5. All unit tests pass, TypeScript compiles cleanly
6. All source files have proper .js extension imports for ESM compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-architecture-governance-core/02-01-SUMMARY.md`
</output>
