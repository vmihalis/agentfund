---
phase: 10-devnet-bootstrap-missing-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - keys/addresses.json
  - keys/collection.json
  - keys/assets.json
  - keys/registration.json
autonomous: false
requirements: [IDENT-01, IDENT-02, IDENT-03, IDENT-04, PAY-02]
user_setup:
  - service: solana-devnet-faucet
    why: "Deployer wallet needs SOL to pay transaction fees for wallet funding, agent registration, and x402 payment tests"
    env_vars: []
    dashboard_config:
      - task: "Request 5 SOL for deployer wallet"
        location: "https://faucet.solana.com -- paste 7GuLR4JgmxsQJAGz3poeCy9Gsp2jUyWntwigZy4iLD8X"

must_haves:
  truths:
    - "Deployer wallet has >= 2 SOL on devnet"
    - "All 4 agent wallets have SOL and USDC ATAs"
    - "4 MPL Core NFTs exist in an AgentFund collection on devnet"
    - "4 AgentIdentityV1 PDAs are registered and verifiable"
    - "x402 payment integration test passes without skip (real on-chain USDC transfer)"
  artifacts:
    - path: "keys/addresses.json"
      provides: "Funded wallet state with non-null ATAs and usdcMint"
      contains: "ata"
    - path: "keys/collection.json"
      provides: "MPL Core collection address"
      contains: "collection"
    - path: "keys/assets.json"
      provides: "Per-agent Core Asset addresses"
      contains: "asset"
    - path: "keys/registration.json"
      provides: "Full registration data with verified:true for all 4 agents"
      contains: "verified"
  key_links:
    - from: "scripts/fund-wallets.ts"
      to: "keys/addresses.json"
      via: "JSON state file write after wallet funding"
      pattern: "addresses\\.json"
    - from: "scripts/register-agents.ts"
      to: "keys/registration.json"
      via: "JSON state file write after on-chain registration"
      pattern: "registration\\.json"
    - from: "tests/integration/x402-payment.test.ts"
      to: "keys/addresses.json"
      via: "walletsAreFunded() check reads ATA state"
      pattern: "walletsAreFunded"
---

<objective>
Fund the deployer wallet on Solana devnet, execute the existing on-chain bootstrap scripts (fund-wallets, register-agents, verify-agents), and run the x402 payment integration test to produce a real on-chain USDC transfer.

Purpose: Close all IDENT-01 through IDENT-04 gaps (on-chain agent state) and PAY-02 gap (real devnet x402 payment) identified in the v1.0 milestone audit. All implementation code already exists -- this is purely operational execution.
Output: Funded wallets, registered agents with verified PDAs, and a passing x402 integration test with real on-chain transaction.
</objective>

<execution_context>
@/Users/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-devnet-bootstrap-missing-verification/10-RESEARCH.md
@.planning/phases/01-solana-foundation-agent-identity/01-VERIFICATION.md
@.planning/phases/06-x402-agent-payment-economy/06-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Fund deployer wallet via Solana devnet faucet</name>
  <files>None</files>
  <action>
    HUMAN ACTION REQUIRED -- Claude cannot access the Solana devnet web faucet.

    The deployer wallet at 7GuLR4JgmxsQJAGz3poeCy9Gsp2jUyWntwigZy4iLD8X has 0 SOL due to prior faucet rate-limiting (HTTP 429). All subsequent scripts require the deployer to have SOL for transaction fees.

    Steps for the user:
    1. Visit https://faucet.solana.com
    2. Paste deployer address: 7GuLR4JgmxsQJAGz3poeCy9Gsp2jUyWntwigZy4iLD8X
    3. Request 5 SOL (can request 2x per hour if needed)
    4. Alternative: Use Solana CLI: solana airdrop 2 7GuLR4JgmxsQJAGz3poeCy9Gsp2jUyWntwigZy4iLD8X --url devnet
    5. Alternative: Visit https://faucet.quicknode.com/solana/devnet

    Resume signal: Type "funded" once deployer has SOL, or provide the balance amount.
  </action>
  <verify>
    <automated>solana balance 7GuLR4JgmxsQJAGz3poeCy9Gsp2jUyWntwigZy4iLD8X --url devnet</automated>
  </verify>
  <done>Deployer wallet shows >= 2 SOL balance on Solana devnet</done>
</task>

<task type="auto">
  <name>Task 2: Execute devnet bootstrap scripts and verify on-chain state</name>
  <files>keys/addresses.json, keys/collection.json, keys/assets.json, keys/registration.json</files>
  <action>
    Run the three existing idempotent scripts in sequence to create all on-chain agent state:

    1. Run `pnpm run fund-wallets` to:
       - Airdrop SOL to all 4 agent wallets (scout, analyzer, treasury, governance)
       - Create or find USDC mint (DEMO_USDC with deployer as authority)
       - Create Associated Token Accounts for all 4 agents
       - Mint 1000 DEMO_USDC to each agent
       - Save complete state to keys/addresses.json

    2. Run `pnpm run register-agents` to:
       - Create "AgentFund Agents" MPL Core collection (saves keys/collection.json)
       - Mint 4 Core Assets, one per agent (saves keys/assets.json)
       - Register 4 AgentIdentityV1 PDAs via mpl-agent-registry (saves keys/registration.json)

    3. Run `pnpm run verify-agents` to:
       - Verify SOL balance, ATA, Core Asset, and Identity PDA for each agent
       - Print PASS/FAIL per check with summary "Agents verified: X/4"

    4. Run integration tests to confirm on-chain state:
       - `pnpm vitest run tests/integration/wallet-setup.test.ts --reporter=verbose` (8 tests, should now pass instead of skip)
       - `pnpm vitest run tests/integration/agent-registration.test.ts --reporter=verbose` (20 tests, should now pass)
       - `pnpm vitest run tests/integration/collection-creation.test.ts --reporter=verbose` (2 tests, should now pass)

    5. Run x402 payment integration test (PAY-02):
       - `pnpm vitest run tests/integration/x402-payment.test.ts --reporter=verbose` (3 tests, all should pass including the 2 that previously skipped)
       - This test performs a real on-chain USDC transfer via x402 protocol and verifies via connection.getTransaction()

    All scripts are idempotent. If any step fails partway, re-running is safe -- scripts check existing state before acting. If devnet is slow, increase timeout or retry.

    If faucet rate-limiting recurs during fund-wallets (airdrop to agent wallets), try:
    - Set SOLANA_RPC_URL env var to a provider RPC with higher limits
    - Use `solana airdrop 2 <agent-address> --url devnet` for individual agents
    - The script handles partial state and can be re-run safely
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/wallet-setup.test.ts tests/integration/agent-registration.test.ts tests/integration/collection-creation.test.ts tests/integration/x402-payment.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - keys/addresses.json has non-null ATAs for all 4 agents and non-null usdcMint (IDENT-02)
    - keys/registration.json exists with verified:true for all 4 agents (IDENT-01, IDENT-03, IDENT-04)
    - keys/collection.json exists with collection address (IDENT-04)
    - verify-agents output shows "Agents verified: 4/4" (IDENT-01, IDENT-03)
    - All 30 integration tests pass (wallet-setup: 8, agent-registration: 20, collection-creation: 2)
    - All 3 x402-payment integration tests pass including real on-chain USDC transfer (PAY-02)
  </done>
</task>

</tasks>

<verification>
Run the complete integration test suite to confirm all on-chain state:
```bash
pnpm vitest run tests/integration/wallet-setup.test.ts tests/integration/agent-registration.test.ts tests/integration/collection-creation.test.ts tests/integration/x402-payment.test.ts --reporter=verbose
```
Expected: 33 tests pass (0 skipped). The x402 payment test produces a real Solana transaction signature verifiable on Solscan.
</verification>

<success_criteria>
- All 4 agent wallets funded with SOL and USDC ATAs on devnet
- 4 MPL Core NFTs in AgentFund collection on devnet
- 4 AgentIdentityV1 PDAs registered and verifiable
- 33 integration tests pass (previously skipped due to unfunded wallets)
- x402 payment produces real on-chain USDC transfer
</success_criteria>

<output>
After completion, create `.planning/phases/10-devnet-bootstrap-missing-verification/10-01-SUMMARY.md`
</output>
