---
phase: 08-frontend-dashboard-sybil-resistance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/tsconfig.json
  - dashboard/next.config.ts
  - dashboard/postcss.config.mjs
  - dashboard/src/app/globals.css
  - dashboard/src/app/layout.tsx
  - dashboard/src/app/page.tsx
  - dashboard/src/app/api/agents/route.ts
  - dashboard/src/app/api/treasury/route.ts
  - dashboard/src/app/api/payments/route.ts
  - dashboard/src/lib/api.ts
  - dashboard/src/lib/types.ts
  - dashboard/src/components/AgentCard.tsx
  - dashboard/src/components/TreasuryPanel.tsx
  - dashboard/src/components/PaymentHistory.tsx
  - pnpm-workspace.yaml
  - tests/unit/dashboard-agents-api.test.ts
  - tests/unit/dashboard-treasury-api.test.ts
  - tests/unit/dashboard-payments.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-05

must_haves:
  truths:
    - "Dashboard displays all 4 agent identities (scout, analyzer, treasury, governance) with correct names and descriptions"
    - "Each agent card includes a clickable Solscan devnet link to the agent's on-chain public key"
    - "Dashboard shows treasury balance (SOL, USDC, total USD value) and LP position data"
    - "Dashboard shows x402 payment history entries with from/to agents, amounts, and Solscan tx links"
  artifacts:
    - path: "dashboard/package.json"
      provides: "Next.js workspace package with dependencies"
      contains: "next"
    - path: "dashboard/src/app/api/agents/route.ts"
      provides: "Agent identity API reading addresses.json"
      exports: ["GET"]
    - path: "dashboard/src/app/api/treasury/route.ts"
      provides: "Treasury balance API proxying to voice server"
      exports: ["GET"]
    - path: "dashboard/src/app/api/payments/route.ts"
      provides: "x402 payment history API"
      exports: ["GET"]
    - path: "dashboard/src/components/AgentCard.tsx"
      provides: "Agent identity card with Solscan link"
      min_lines: 20
    - path: "dashboard/src/components/TreasuryPanel.tsx"
      provides: "Treasury balance and LP display"
      min_lines: 30
    - path: "dashboard/src/components/PaymentHistory.tsx"
      provides: "x402 payment log table"
      min_lines: 20
  key_links:
    - from: "dashboard/src/app/page.tsx"
      to: "/api/agents"
      via: "fetch in AgentCard rendering"
      pattern: "fetch.*api/agents"
    - from: "dashboard/src/app/api/agents/route.ts"
      to: "../keys/addresses.json"
      via: "fs.readFileSync"
      pattern: "readFileSync.*addresses"
    - from: "dashboard/src/app/api/treasury/route.ts"
      to: "http://localhost:4003"
      via: "fetch proxy to voice server"
      pattern: "fetch.*localhost.*4003"
---

<objective>
Create the Next.js dashboard workspace with API routes and core data display components for agent identities, treasury state, and payment history.

Purpose: Establish the dashboard foundation that all subsequent plans build on -- workspace config, API data layer, and the three static/data-driven panels (agents, treasury, payments).
Output: Working dashboard at localhost:3000 showing agent cards with Solscan links, treasury balance panel, and x402 payment history.
</objective>

<execution_context>
@/Users/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-dashboard-sybil-resistance/08-RESEARCH.md

@src/types/agents.ts
@src/types/proposals.ts
@src/lib/x402/types.ts
@src/voice/voice-server.ts
@keys/addresses.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/agents.ts:
```typescript
export const AGENT_ROLES = ['scout', 'analyzer', 'treasury', 'governance'] as const;
export type AgentRole = (typeof AGENT_ROLES)[number];
export interface AgentConfig {
  role: AgentRole;
  name: string;
  description: string;
  metadataUri?: string;
}
export const AGENT_CONFIGS: Record<AgentRole, AgentConfig>;
```

From src/types/proposals.ts:
```typescript
export interface TreasuryBalance {
  solBalance: number;
  usdcBalance: number;
  totalValueUsd: number;
  lpPositions?: LPPosition[];
}
export interface LPPosition {
  poolAddress: string;
  positionAddress: string;
  tokenX: string;
  tokenY: string;
  liquidityShare: number;
  unclaimedFees: number;
}
```

From keys/addresses.json (shape):
```json
{
  "deployer": "7GuLR4JgmxsQJAGz3poeCy9Gsp2jUyWntwigZy4iLD8X",
  "agents": {
    "scout": { "publicKey": "EMKvtgEGf91t4voCE7bF4MgCYU46ubJiRjJn9jmRRcej", "ata": null },
    "analyzer": { "publicKey": "DeUfaAWhauYzPQNetQF8NHDFY8hiQXazE4HYBBAwFMfu", "ata": null },
    "treasury": { "publicKey": "7vmyrNkxfegeT3146qr1mf9CZ3s8zYt1kfcgRrqgst9L", "ata": null },
    "governance": { "publicKey": "2pVL2sZ4oMKh9J3tDHvrdfUxcf4JBVxi3F8pfzFUb2PB", "ata": null }
  },
  "usdcMint": null,
  "isDemoUSDC": false
}
```

From src/lib/x402/types.ts:
```typescript
export interface PaymentRequirements {
  x402Version: 1;
  scheme: 'exact';
  network: string;
  payment: { recipientWallet: string; tokenAccount: string; mint: string; amount: number; amountUSDC: number; };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js dashboard workspace with API routes</name>
  <files>
    pnpm-workspace.yaml,
    dashboard/package.json,
    dashboard/tsconfig.json,
    dashboard/next.config.ts,
    dashboard/postcss.config.mjs,
    dashboard/src/app/globals.css,
    dashboard/src/app/layout.tsx,
    dashboard/src/app/page.tsx,
    dashboard/src/app/api/agents/route.ts,
    dashboard/src/app/api/treasury/route.ts,
    dashboard/src/app/api/payments/route.ts,
    dashboard/src/lib/api.ts,
    dashboard/src/lib/types.ts,
    tests/unit/dashboard-agents-api.test.ts,
    tests/unit/dashboard-treasury-api.test.ts,
    tests/unit/dashboard-payments.test.ts
  </files>
  <action>
    1. Update pnpm-workspace.yaml to add `dashboard` to the packages list (currently has no packages field -- the file only contains onlyBuiltDependencies config). Add `packages: ['dashboard']`.

    2. Create dashboard/ directory manually (do NOT use create-next-app -- too slow and interactive). Create these files directly:

    **dashboard/package.json:** name "dashboard", private true, type "module", scripts: { dev: "next dev", build: "next build", start: "next start" }. Dependencies: next@^15, react@^19, react-dom@^19, tailwindcss@^4, @tailwindcss/postcss@^4. DevDependencies: @types/node@^25, @types/react@^19, @types/react-dom@^19, typescript@~5.4.5.

    **dashboard/tsconfig.json:** Standard Next.js App Router tsconfig -- target ES2022, lib ["dom","dom.iterable","esnext"], module "esnext", moduleResolution "bundler", jsx "preserve", strict true, noEmit true, incremental true, paths: { "@/*": ["./src/*"] }. Include ["next-env.d.ts","**/*.ts","**/*.tsx"]. Plugins: [{ name: "next" }].

    **dashboard/next.config.ts:** Minimal config. Export default { /* empty for now */ }. Use `import type { NextConfig } from 'next'`.

    **dashboard/postcss.config.mjs:** Export default { plugins: { '@tailwindcss/postcss': {} } }.

    **dashboard/src/app/globals.css:** Import tailwind: `@import "tailwindcss";`. Add base dark theme styles: body with bg-gray-950, text-gray-100.

    **dashboard/src/app/layout.tsx:** Root layout importing globals.css. Dark background (bg-gray-950), font-sans. Title: "AgentFund Dashboard". Description: "Autonomous AI agent treasury management".

    **dashboard/src/lib/types.ts:** Dashboard-specific types replicating the backend shapes (do NOT import from root src/ -- Next.js has its own module resolution):
    - `AgentInfo`: { role, name, description, publicKey, solscanUrl }
    - `TreasuryData`: { solBalance, usdcBalance, totalValueUsd, lpPositions: LPPositionData[] }
    - `LPPositionData`: { poolAddress, positionAddress, tokenX, tokenY, liquidityShare, unclaimedFees }
    - `PaymentRecord`: { timestamp, from, to, amount, service, txSignature, txUrl }

    **dashboard/src/lib/api.ts:** Fetch helpers for each API route. Functions: fetchAgents() -> AgentInfo[], fetchTreasury() -> TreasuryData, fetchPayments() -> PaymentRecord[]. Each uses relative URL `/api/{route}` with error handling (try/catch, return empty/default on failure). No external dependencies.

    **dashboard/src/app/api/agents/route.ts:** GET handler. Read `../keys/addresses.json` using `fs.readFileSync` with `path.join(process.cwd(), '..', 'keys', 'addresses.json')`. Map each agent from AGENT_CONFIGS (hardcode the 4 agent configs matching src/types/agents.ts AGENT_CONFIGS exactly) with their publicKey from addresses.json. Construct solscanUrl as `https://solscan.io/address/${publicKey}?cluster=devnet`. Return JSON array. Handle missing file gracefully (return fallback with 'unknown' keys).

    **dashboard/src/app/api/treasury/route.ts:** GET handler. Proxy to voice server: `fetch('http://localhost:${process.env.VOICE_SERVER_PORT || 4003}/api/voice/command', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text: 'check treasury' }) })`. Parse the response and extract treasury data from the result. Return TreasuryData shape. On failure (server not running), return demo fallback: { solBalance: 1.5, usdcBalance: 100, totalValueUsd: 325, lpPositions: [] }.

    **dashboard/src/app/api/payments/route.ts:** GET handler. Return demo payment history data (static array of PaymentRecord entries representing example x402 payments between agents). Include 3-4 entries showing scout and analyzer payments with realistic timestamps, amounts (0.001 and 0.002 USDC matching Phase 6 pricing), and placeholder tx signatures. Format txUrl as Solscan devnet links. This is demo/static data since there is no persistent payment log in the backend -- the x402 transactions are on-chain but not indexed.

    **dashboard/src/app/page.tsx:** Placeholder main page that will be fleshed out in Task 2. Import and render AgentCard grid, TreasuryPanel, and PaymentHistory. Use `'use client'` directive. Fetch data via the api.ts helpers in useEffect.

    3. Create unit tests in the ROOT tests/ directory (not in dashboard/ -- the root vitest.config.ts runs tests from tests/**/*.test.ts):

    **tests/unit/dashboard-agents-api.test.ts:** Test the agent data mapping logic (not the Next.js route handler directly -- extract the mapping function or test the data shape). Mock fs.readFileSync to return the addresses.json fixture. Verify: returns 4 agents, each has role/name/description/publicKey/solscanUrl, solscanUrl contains `?cluster=devnet`, roles match ['scout','analyzer','treasury','governance'].

    **tests/unit/dashboard-treasury-api.test.ts:** Test the treasury data shape. Mock fetch to return a voice server response. Verify: response matches TreasuryData shape with solBalance, usdcBalance, totalValueUsd. Test fallback: when fetch fails, returns demo data.

    **tests/unit/dashboard-payments.test.ts:** Test payment history data shape. Verify: each record has timestamp, from, to, amount, service, txSignature, txUrl. Verify txUrl contains `solscan.io/tx/`. Verify amounts match expected x402 pricing (0.001 or 0.002).

    4. Run `cd /Users/memehalis/hack/projects/agentfund && pnpm install` to install dashboard workspace dependencies.

    IMPORTANT: The test files should test extracted pure functions (data mapping, shape validation) rather than the Next.js route handlers directly (which require the Next.js runtime). Create small testable utility functions in dashboard/src/lib/ that the API routes call, and test those.
  </action>
  <verify>
    <automated>cd /Users/memehalis/hack/projects/agentfund && pnpm install && pnpm vitest run tests/unit/dashboard-agents-api.test.ts tests/unit/dashboard-treasury-api.test.ts tests/unit/dashboard-payments.test.ts -x</automated>
  </verify>
  <done>
    - pnpm-workspace.yaml includes dashboard package
    - dashboard/package.json exists with next, react, tailwindcss dependencies
    - Three API routes exist (agents, treasury, payments) with GET handlers
    - lib/types.ts defines AgentInfo, TreasuryData, LPPositionData, PaymentRecord
    - lib/api.ts provides fetch helpers for all three endpoints
    - Three unit tests pass validating data shapes and mapping logic
    - `pnpm install` succeeds with dashboard workspace resolved
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AgentCard, TreasuryPanel, and PaymentHistory components</name>
  <files>
    dashboard/src/components/AgentCard.tsx,
    dashboard/src/components/TreasuryPanel.tsx,
    dashboard/src/components/PaymentHistory.tsx,
    dashboard/src/app/page.tsx
  </files>
  <action>
    Create three client components and wire them into the main dashboard page.

    **dashboard/src/components/AgentCard.tsx:** `'use client'` component. Props: AgentInfo (from lib/types.ts). Renders a dark card (bg-gray-900 border border-gray-800 rounded-lg p-4) with:
    - Agent name as heading (text-lg font-semibold)
    - Agent role as badge (uppercase, small text, colored per role: scout=green, analyzer=blue, treasury=yellow, governance=purple)
    - Agent description (text-gray-400 text-sm)
    - Public key truncated (first 4 + ... + last 4 chars) with a "View on Solscan" link (external, opens new tab, text-cyan-400 hover:underline) pointing to solscanUrl
    - Copy-to-clipboard button for full public key

    **dashboard/src/components/TreasuryPanel.tsx:** `'use client'` component. Props: TreasuryData (from lib/types.ts). Renders a dark panel (bg-gray-900 border border-gray-800 rounded-lg p-6) with:
    - Section title "Treasury Status"
    - Three stat boxes in a row: SOL balance, USDC balance, Total USD value (formatted with $ and 2 decimal places)
    - LP Positions subsection: if lpPositions exists and length > 0, render a table with pool address (truncated), token pair, liquidity share, unclaimed fees. Each pool address links to Solscan. If no positions, show "No active LP positions"
    - Loading state: show skeleton/placeholder when data is undefined

    **dashboard/src/components/PaymentHistory.tsx:** `'use client'` component. Props: PaymentRecord[] (from lib/types.ts). Renders a dark panel with:
    - Section title "x402 Payment History"
    - Table with columns: Time (relative, e.g., "2 min ago"), From (agent role), To (agent role), Amount (USDC), Service, Tx (link to Solscan)
    - Each row styled with alternating bg-gray-900/bg-gray-950
    - If no payments, show "No payments recorded"

    **dashboard/src/app/page.tsx:** Update to be the main dashboard. `'use client'` directive. Layout:
    - Header: "AgentFund Dashboard" title, subtitle "Autonomous AI Treasury Management"
    - Grid section "Agent Identities" (2x2 grid on lg, 1 col on mobile) rendering AgentCard for each agent
    - Full-width TreasuryPanel section
    - Full-width PaymentHistory section
    - Use useEffect + useState to fetch from api.ts helpers on mount. Show loading states while fetching.
    - Tailwind dark theme throughout: bg-gray-950 main background, components use bg-gray-900

    All components must be responsive (stack on mobile, grid on desktop). No external component library -- pure Tailwind utility classes.
  </action>
  <verify>
    <automated>cd /Users/memehalis/hack/projects/agentfund/dashboard && pnpm next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - AgentCard renders agent info with Solscan link and truncated public key
    - TreasuryPanel displays SOL, USDC, total USD, and LP positions table
    - PaymentHistory renders x402 payment log with Solscan tx links
    - Main dashboard page composes all three components in responsive grid layout
    - `next build` succeeds with no TypeScript or build errors
    - Dark theme applied consistently across all components
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/dashboard-agents-api.test.ts tests/unit/dashboard-treasury-api.test.ts tests/unit/dashboard-payments.test.ts -x` -- all pass
- `cd dashboard && pnpm next build` -- succeeds with no errors
- API routes return correct JSON shapes when dashboard runs with `pnpm dev`
</verification>

<success_criteria>
- Dashboard workspace exists as pnpm workspace package
- 4 agent cards display with correct names, descriptions, and Solscan devnet links
- Treasury panel shows balance data (SOL, USDC, total USD)
- Payment history shows x402 inter-agent payment records with tx links
- 3 unit tests pass validating data layer
- Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-dashboard-sybil-resistance/08-01-SUMMARY.md`
</output>
