---
phase: 08-frontend-dashboard-sybil-resistance
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - dashboard/src/app/submit/page.tsx
  - dashboard/src/components/PassportGate.tsx
  - dashboard/src/components/ProposalForm.tsx
  - dashboard/src/app/api/proposals/submit/route.ts
  - dashboard/src/lib/types.ts
  - dashboard/package.json
  - tests/unit/passport-gate.test.ts
autonomous: true
requirements:
  - SYBIL-01
  - SYBIL-02

user_setup:
  - service: human-passport
    why: "Sybil resistance for proposal submission"
    env_vars:
      - name: NEXT_PUBLIC_PASSPORT_API_KEY
        source: "Human Passport Developer Portal (https://developer.passport.xyz) -> API Keys"
      - name: NEXT_PUBLIC_PASSPORT_SCORER_ID
        source: "Human Passport Developer Portal -> Scorers -> Create Scorer -> copy ID"
    dashboard_config:
      - task: "Register for Human Passport API access"
        location: "https://developer.passport.xyz"

must_haves:
  truths:
    - "Proposal submission page at /submit exists and is accessible from the dashboard"
    - "Unverified users see the Passport verification widget and cannot access the proposal form"
    - "Users with humanity score >= 20 can see and submit the proposal form"
    - "Submitted proposals appear in the pipeline as 'submitted' stage"
  artifacts:
    - path: "dashboard/src/app/submit/page.tsx"
      provides: "Proposal submission page with Passport gate"
      min_lines: 20
    - path: "dashboard/src/components/PassportGate.tsx"
      provides: "Human Passport verification wrapper component"
      min_lines: 30
    - path: "dashboard/src/components/ProposalForm.tsx"
      provides: "Proposal submission form"
      min_lines: 30
    - path: "dashboard/src/app/api/proposals/submit/route.ts"
      provides: "POST handler for proposal submission"
      exports: ["POST"]
  key_links:
    - from: "dashboard/src/components/PassportGate.tsx"
      to: "@human.tech/passport-embed"
      via: "usePassportScore hook"
      pattern: "usePassportScore|PassportScoreWidget"
    - from: "dashboard/src/app/submit/page.tsx"
      to: "dashboard/src/components/PassportGate.tsx"
      via: "wraps ProposalForm in PassportGate"
      pattern: "PassportGate"
    - from: "dashboard/src/components/ProposalForm.tsx"
      to: "/api/proposals/submit"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/proposals/submit"
---

<objective>
Add Human Passport sybil resistance gating the proposal submission page.

Purpose: Ensure only verified humans (humanity score >= 20) can submit funding proposals, satisfying the Human Passport bounty requirements.
Output: /submit page with Passport verification widget that gates access to the proposal form.
</objective>

<execution_context>
@/Users/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-dashboard-sybil-resistance/08-RESEARCH.md
@.planning/phases/08-frontend-dashboard-sybil-resistance/08-01-SUMMARY.md

<interfaces>
<!-- Human Passport Embed API from research -->

From @human.tech/passport-embed:
```typescript
import { PassportScoreWidget, usePassportScore, DarkTheme } from '@human.tech/passport-embed';

// usePassportScore hook
const { score, isPassing, loading } = usePassportScore({
  apiKey: string,       // NEXT_PUBLIC_PASSPORT_API_KEY
  scorerId: string,     // NEXT_PUBLIC_PASSPORT_SCORER_ID
  address?: string,     // Wallet address (optional for some flows)
});

// PassportScoreWidget component
<PassportScoreWidget
  apiKey={string}
  scorerId={string}
  address={string}
  generateSignatureCallback={async (message: string) => string}
  theme={DarkTheme}
  collapseMode="off"
/>
```

From dashboard/src/lib/types.ts (Plan 01):
```typescript
export interface PipelineProposal {
  id: string;
  title: string;
  stage: 'submitted' | 'evaluating' | 'approved' | 'funded';
  updatedAt: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Passport Embed and create PassportGate + ProposalForm</name>
  <files>
    dashboard/package.json,
    dashboard/src/components/PassportGate.tsx,
    dashboard/src/components/ProposalForm.tsx,
    dashboard/src/app/api/proposals/submit/route.ts,
    dashboard/src/app/submit/page.tsx,
    dashboard/src/app/page.tsx,
    dashboard/src/lib/types.ts,
    tests/unit/passport-gate.test.ts
  </files>
  <action>
    1. Add @human.tech/passport-embed to dashboard/package.json dependencies. Run pnpm install.

    2. **dashboard/src/lib/types.ts:** Add type:
    - `ProposalSubmission`: { title: string, description: string, requestedAmount: number, teamInfo: string }

    3. **dashboard/src/components/PassportGate.tsx:** `'use client'` component.

    Props: { children: React.ReactNode, onVerified?: (score: number) => void }

    **Implementation:**
    - Use `usePassportScore` hook with apiKey from `process.env.NEXT_PUBLIC_PASSPORT_API_KEY` and scorerId from `process.env.NEXT_PUBLIC_PASSPORT_SCORER_ID`.
    - State: verified (boolean), demoMode (boolean). Check env vars on mount -- if either is missing/empty, enable demo mode.

    **Demo Mode (env vars not set):** Show a warning banner "Passport API keys not configured -- demo mode active" with a "Simulate Verification" button. Clicking the button sets verified=true and calls onVerified(25). This allows the demo to proceed at the hackathon even without Passport credentials configured. The demo mode banner should be styled distinctly (yellow/amber border) so judges understand it is a fallback.

    **Production Mode (env vars set):** Render PassportScoreWidget from @human.tech/passport-embed with DarkTheme. Show humanity score progress (loading -> score display). When isPassing is true (score >= 20), show a success message "Humanity verified (score: {score})" and render children. When isPassing is false, block children and show the verification widget with instructions: "Verify your humanity to submit proposals. Minimum score: 20."

    **For generateSignatureCallback:** For hackathon, implement a stub that returns empty string (wallet connection is a v2 concern per research open question). Add a comment noting production would connect to user's browser wallet.

    4. **dashboard/src/components/ProposalForm.tsx:** `'use client'` component.

    Props: { onSubmit?: (proposal: ProposalSubmission) => void }

    Form fields (all required):
    - Title (text input, 3-100 chars)
    - Description (textarea, 20-2000 chars)
    - Requested Amount (number input, min 1, labeled "USDC")
    - Team Info (textarea, 10-500 chars)
    - Submit button (styled, disabled while submitting)

    On submit: POST to /api/proposals/submit with the form data. Show success message with "Proposal submitted! Track it in the pipeline." and a link back to the main dashboard. Show error message on failure. Call onSubmit callback if provided.

    Validation: Client-side validation for field lengths before submit. Display inline errors.

    Styling: Dark theme matching dashboard (bg-gray-900, border-gray-800). Input fields with bg-gray-800, text-gray-100, focus ring cyan-500.

    5. **dashboard/src/app/api/proposals/submit/route.ts:** POST handler. Accept ProposalSubmission body. Validate required fields (title, description, requestedAmount, teamInfo). Generate an ID (crypto.randomUUID). Return { success: true, proposal: { id, title, stage: 'submitted', updatedAt: Date.now() } }. This is a demo endpoint -- it does not persist to a database, but the response confirms the submission shape.

    6. **dashboard/src/app/submit/page.tsx:** The submission page. Layout:
    - Back link to main dashboard "< Back to Dashboard"
    - Title: "Submit a Proposal"
    - Subtitle: "Propose a project for AI agent evaluation and funding"
    - PassportGate wrapping ProposalForm
    - Unverified users see only the Passport verification UI
    - Verified users see the full form

    7. **dashboard/src/app/page.tsx:** Add a "Submit Proposal" navigation link/button in the header that navigates to /submit. Use Next.js Link component.

    8. **tests/unit/passport-gate.test.ts:** Test the gating logic as a pure function. Extract a helper function `shouldAllowSubmission(score: number | null, isPassing: boolean, demoMode: boolean): boolean` into a testable module (e.g., dashboard/src/lib/passport-utils.ts). Tests:
    - score >= 20 and isPassing = true -> allow
    - score < 20 and isPassing = false -> block
    - score null (loading) -> block
    - demoMode = true with no score -> block (until user clicks simulate)
    - demoMode = true with simulated score >= 20 -> allow
    Test at least 5 cases covering the boundary conditions.
  </action>
  <verify>
    <automated>cd /Users/memehalis/hack/projects/agentfund && pnpm install && pnpm vitest run tests/unit/passport-gate.test.ts -x && cd dashboard && pnpm next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - @human.tech/passport-embed installed in dashboard workspace
    - PassportGate component blocks unverified users and shows Passport widget
    - PassportGate demo mode works when env vars are not set
    - ProposalForm validates input and POSTs to /api/proposals/submit
    - /submit page wraps form in PassportGate
    - Dashboard header has "Submit Proposal" link to /submit
    - passport-gate unit tests pass (5+ cases)
    - next build succeeds
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/passport-gate.test.ts -x` -- passes
- `cd dashboard && pnpm next build` -- succeeds
- /submit page renders PassportGate blocking access
- In demo mode (no env vars), simulate verification button unblocks form
- Form submission POSTs valid data to /api/proposals/submit
</verification>

<success_criteria>
- Human Passport widget renders on /submit page (or demo mode fallback)
- Unverified users cannot access the proposal submission form
- Verified users (score >= 20) can submit proposals
- Proposal form validates and submits to API
- Dashboard links to /submit page
- 5+ unit tests pass for gating logic
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-dashboard-sybil-resistance/08-03-SUMMARY.md`
</output>
