---
phase: 10-devnet-bootstrap-missing-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-scout-agent/03-VERIFICATION.md
autonomous: true
requirements: [SCOUT-01, SCOUT-02, SCOUT-03]

must_haves:
  truths:
    - "Scout agent discovers grant proposals via Unbrowse intent resolution (SCOUT-01)"
    - "Scout calls Unbrowse /v1/intent/resolve with natural language intents (SCOUT-02)"
    - "Scout returns structured proposal data (title, description, amount, team info) to Governance Agent (SCOUT-03)"
  artifacts:
    - path: ".planning/phases/03-scout-agent/03-VERIFICATION.md"
      provides: "Formal verification report for Phase 3 Scout Agent requirements"
      contains: "SCOUT-01"
      min_lines: 80
  key_links:
    - from: "src/agents/scout-agent.ts"
      to: "src/lib/unbrowse/client.ts"
      via: "constructor injection of UnbrowseClient"
      pattern: "UnbrowseClient"
    - from: "src/agents/scout-agent.ts"
      to: "src/lib/unbrowse/parser.ts"
      via: "parseUnbrowseResult for response normalization"
      pattern: "parseUnbrowseResult"
    - from: "src/agents/scout-agent.ts"
      to: "src/agents/stubs/stub-scout.ts"
      via: "STUB_PROPOSALS fallback import"
      pattern: "STUB_PROPOSALS"
---

<objective>
Create the formal 03-VERIFICATION.md for Phase 3 (Scout Agent) by running existing unit tests, inspecting source artifacts, verifying key links, and documenting requirement satisfaction evidence.

Purpose: Close the SCOUT-01, SCOUT-02, SCOUT-03 "orphaned" requirement gap identified in the v1.0 milestone audit. The Scout Agent code is fully implemented and tested (33 tests pass) but was never formally verified. This plan creates the missing verification document.
Output: .planning/phases/03-scout-agent/03-VERIFICATION.md with PASSED status and all 3 SCOUT requirements mapped to evidence.
</objective>

<execution_context>
@/Users/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scout-agent/03-01-SUMMARY.md
@.planning/phases/03-scout-agent/03-01-PLAN.md
@.planning/phases/04-proposal-analyzer-agent/04-VERIFICATION.md

<interfaces>
<!-- Source files to verify (from Phase 3 SUMMARY) -->

From src/lib/unbrowse/types.ts:
  - UnbrowseRequest, UnbrowseResponse interfaces
  - GRANT_TARGETS constant

From src/lib/unbrowse/client.ts:
  - UnbrowseClient class with resolveIntent() and healthCheck()

From src/lib/unbrowse/parser.ts:
  - parseUnbrowseResult(), parseAmount(), extractResultArray()
  - Zod validation schemas

From src/agents/scout-agent.ts:
  - ScoutAgent extends BaseAgent implements IScoutAgent
  - 3-layer fallback: live Unbrowse -> cache -> STUB_PROPOSALS

Test files to run:
  - tests/unit/unbrowse-parser.test.ts (20 tests)
  - tests/unit/scout-agent.test.ts (13 tests)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run tests and gather verification evidence</name>
  <files>None (read-only inspection)</files>
  <action>
    Gather all evidence needed for the verification report:

    1. Run Scout unit tests with verbose output:
       - `pnpm vitest run tests/unit/scout-agent.test.ts --reporter=verbose`
       - `pnpm vitest run tests/unit/unbrowse-parser.test.ts --reporter=verbose`
       - Record exact pass/fail counts

    2. Read and inspect source artifacts for substance (not stubs):
       - src/lib/unbrowse/types.ts -- confirm UnbrowseRequest, UnbrowseResponse, GRANT_TARGETS exports
       - src/lib/unbrowse/client.ts -- confirm resolveIntent() calls /v1/intent/resolve, healthCheck() exists
       - src/lib/unbrowse/parser.ts -- confirm parseUnbrowseResult() normalizes data into Proposal[], Zod schemas exist
       - src/lib/unbrowse/index.ts -- confirm barrel exports
       - src/agents/scout-agent.ts -- confirm extends BaseAgent, implements IScoutAgent, 3-layer fallback
       - src/agents/stubs/stub-scout.ts -- confirm STUB_PROPOSALS named export
       - Record line counts for each file

    3. Verify key links (imports/exports wiring):
       - scout-agent.ts imports UnbrowseClient from unbrowse/client
       - scout-agent.ts imports parseUnbrowseResult from unbrowse/parser
       - scout-agent.ts imports STUB_PROPOSALS from stubs/stub-scout
       - scout-agent.ts implements IScoutAgent from agents/types
       - unbrowse/client.ts imports types from unbrowse/types
       - agents/index.ts re-exports ScoutAgent
       - Record exact import lines

    4. Check for anti-patterns:
       - Scan for TODO, FIXME, PLACEHOLDER, HACK in all Phase 3 source files
       - Scan for stub returns (return null, return {}, return []) in production code
       - Record findings

    5. Run full test suite to confirm no regressions:
       - `pnpm test` -- record total pass count
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/scout-agent.test.ts tests/unit/unbrowse-parser.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 33 Scout-related tests pass. Source artifacts inspected. Key links verified. Anti-pattern scan clean. Evidence collected for verification report.</done>
</task>

<task type="auto">
  <name>Task 2: Create 03-VERIFICATION.md with requirement evidence</name>
  <files>.planning/phases/03-scout-agent/03-VERIFICATION.md</files>
  <action>
    Create the formal Phase 3 verification report following the project's standard format (use 04-VERIFICATION.md as template). The report must include:

    1. **Frontmatter** with phase, verified timestamp, status: passed, score, re_verification: false

    2. **Observable Truths** table mapping Phase 3 success criteria from ROADMAP.md:
       - Truth 1 (SCOUT-01): "Issuing a 'find grant proposals' command to Scout triggers an Unbrowse intent resolution call to localhost:6969 and returns real web data" -- map to ScoutAgent.discoverProposals() calling UnbrowseClient.resolveIntent(), with test evidence
       - Truth 2 (SCOUT-02): "Scout returns structured proposal objects (title, description, amount, team info) to the Governance Agent via the event bus" -- map to parseUnbrowseResult() and event emission, with test evidence
       - Truth 3 (SCOUT-03): "Scout handles Unbrowse unavailability gracefully with stub/cached data so the demo pipeline never breaks" -- map to 3-layer fallback chain, with test evidence

    3. **Required Artifacts** table with file paths, line counts, exports, and verification status

    4. **Key Link Verification** table with From/To/Via/Status/Details showing exact import lines

    5. **Requirements Coverage** table mapping SCOUT-01, SCOUT-02, SCOUT-03 to evidence:
       - SCOUT-01: ScoutAgent.discoverProposals() calls UnbrowseClient.resolveIntent() which POSTs to /v1/intent/resolve; 13 ScoutAgent tests + 8 client tests verify
       - SCOUT-02: UnbrowseClient.resolveIntent() sends POST with intent + target URL; 20 parser tests verify response normalization
       - SCOUT-03: parseUnbrowseResult() normalizes Unbrowse responses into typed Proposal[]; 20 parser tests + ScoutAgent fallback tests verify

    6. **Anti-Patterns Found** section (should be clean based on evidence)

    7. **Human Verification Required** section for live Unbrowse integration (optional -- ScoutAgent falls back gracefully)

    8. **Test Suite Summary** with exact test counts and pass status

    Use evidence gathered in Task 1. Do NOT fabricate line numbers or test counts -- use actual values from inspection. Follow 04-VERIFICATION.md structure exactly.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-scout-agent/03-VERIFICATION.md && grep -c "SCOUT-0[123]" .planning/phases/03-scout-agent/03-VERIFICATION.md</automated>
  </verify>
  <done>
    - .planning/phases/03-scout-agent/03-VERIFICATION.md exists with status: passed
    - All 3 SCOUT requirements (SCOUT-01, SCOUT-02, SCOUT-03) mapped to code evidence
    - Observable truths from ROADMAP.md Phase 3 success criteria verified
    - Key links documented with exact import lines
    - Anti-pattern scan documented
    - File follows project's standard VERIFICATION.md format (matching 04-VERIFICATION.md structure)
  </done>
</task>

</tasks>

<verification>
Verify the Phase 3 verification report is complete and accurate:
```bash
# Confirm file exists and has substantial content
wc -l .planning/phases/03-scout-agent/03-VERIFICATION.md

# Confirm all 3 SCOUT requirements are covered
grep "SCOUT-01\|SCOUT-02\|SCOUT-03" .planning/phases/03-scout-agent/03-VERIFICATION.md

# Confirm status is passed
grep "status: passed" .planning/phases/03-scout-agent/03-VERIFICATION.md

# Confirm all Scout tests still pass
pnpm vitest run tests/unit/scout-agent.test.ts tests/unit/unbrowse-parser.test.ts --reporter=verbose
```
</verification>

<success_criteria>
- .planning/phases/03-scout-agent/03-VERIFICATION.md exists with status: passed
- All 3 SCOUT requirements have SATISFIED status with code evidence
- 33 Scout-related tests pass (13 ScoutAgent + 20 parser)
- SCOUT-01, SCOUT-02, SCOUT-03 are no longer "orphaned" in the milestone audit
</success_criteria>

<output>
After completion, create `.planning/phases/10-devnet-bootstrap-missing-verification/10-02-SUMMARY.md`
</output>
