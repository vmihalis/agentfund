---
phase: 06-x402-agent-payment-economy
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/servers/scout-server.ts
  - src/servers/analyzer-server.ts
  - src/servers/start-servers.ts
  - tests/unit/scout-server.test.ts
  - tests/unit/analyzer-server.test.ts
  - tests/integration/x402-payment.test.ts
autonomous: true
requirements:
  - SCOUT-04
  - ANLZ-04
  - PAY-02

must_haves:
  truths:
    - "Scout has an HTTP endpoint at /discover gated by x402 middleware that returns proposals on valid payment"
    - "Analyzer has an HTTP endpoint at /evaluate gated by x402 middleware that returns evaluations on valid payment"
    - "Calling a gated endpoint without payment returns 402 with payment requirements"
    - "An agent using wrapFetch can pay for and receive data from a peer's x402-gated endpoint"
    - "At least one x402 payment produces a real on-chain devnet USDC transfer"
  artifacts:
    - path: "src/servers/scout-server.ts"
      provides: "Express app with /discover endpoint gated by x402 middleware"
      exports: ["createScoutServer"]
    - path: "src/servers/analyzer-server.ts"
      provides: "Express app with /evaluate endpoint gated by x402 middleware"
      exports: ["createAnalyzerServer"]
    - path: "src/servers/start-servers.ts"
      provides: "Script to start both agent HTTP servers"
    - path: "tests/integration/x402-payment.test.ts"
      provides: "End-to-end x402 payment cycle test"
  key_links:
    - from: "src/servers/scout-server.ts"
      to: "src/lib/x402/middleware.ts"
      via: "x402Middleware wrapping /discover route"
      pattern: "x402Middleware"
    - from: "src/servers/scout-server.ts"
      to: "src/agents/scout-agent.ts"
      via: "ScoutAgent.discoverProposals called in route handler"
      pattern: "scout\\.discoverProposals"
    - from: "src/servers/analyzer-server.ts"
      to: "src/lib/x402/middleware.ts"
      via: "x402Middleware wrapping /evaluate route"
      pattern: "x402Middleware"
    - from: "src/servers/analyzer-server.ts"
      to: "src/agents/analyzer-agent.ts"
      via: "AnalyzerAgent.evaluateProposal called in route handler"
      pattern: "analyzer\\.evaluateProposal"
    - from: "tests/integration/x402-payment.test.ts"
      to: "src/lib/x402/client.ts"
      via: "wrapFetchWithPayment making paid request"
      pattern: "wrapFetchWithPayment"
---

<objective>
Create Express HTTP servers for Scout and Analyzer agents gated by x402 payment middleware, plus an integration test proving the full x402 payment cycle with on-chain USDC transfer.

Purpose: Makes Scout and Analyzer services purchasable by peer agents over HTTP. The Governance Agent (or any agent with USDC) can call these endpoints via wrapFetch and automatically pay for the service. This completes the x402 agent payment economy.

Output: `src/servers/` directory with scout-server.ts, analyzer-server.ts, start-servers.ts. Integration test verifying full 402->pay->content cycle.
</objective>

<execution_context>
@/Users/memehalis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/memehalis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-x402-agent-payment-economy/06-RESEARCH.md
@.planning/phases/06-x402-agent-payment-economy/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan consumes. -->

From src/lib/x402/index.ts (created in Plan 01):
```typescript
export { x402Middleware } from './middleware.js';
export { wrapFetchWithPayment } from './client.js';
export { verifyAndSettlePayment } from './verify.js';
export type { X402Config, PaymentRequirements, PaymentProof, VerifyResult, WrapFetchOptions } from './types.js';
```

From src/lib/x402/types.ts (created in Plan 01):
```typescript
export interface X402Config {
  recipientWallet: PublicKey;
  usdcMint: PublicKey;
  priceUsdc: number;  // Base units (1 USDC = 1_000_000)
  connection: Connection;
  cluster: 'devnet' | 'mainnet-beta';
}
```

From src/agents/scout-agent.ts:
```typescript
export class ScoutAgent extends BaseAgent implements IScoutAgent {
  constructor(bus: AgentEventBus, unbrowseClient?: UnbrowseClient);
  async initialize(): Promise<void>;
  async discoverProposals(query: string): Promise<Proposal[]>;
}
```

From src/agents/analyzer-agent.ts:
```typescript
export class AnalyzerAgent extends BaseAgent implements IAnalyzerAgent {
  constructor(bus: AgentEventBus, client?: Anthropic);
  async initialize(): Promise<void>;
  async evaluateProposal(proposal: Proposal): Promise<Evaluation>;
}
```

From src/events/index.ts:
```typescript
export { TypedEventBus } from './event-bus.js';
export type { AgentEventBus } from './event-types.js';
```

From src/lib/keys.ts:
```typescript
export function getWeb3Keypair(role: AgentRole): Keypair;
```

From src/lib/solana/index.ts:
```typescript
export { getConnection } from './connection.js';
export { DEVNET_USDC_MINT } from './token-accounts.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Scout and Analyzer Express servers with x402 gating</name>
  <files>src/servers/scout-server.ts, src/servers/analyzer-server.ts, src/servers/start-servers.ts, tests/unit/scout-server.test.ts, tests/unit/analyzer-server.test.ts</files>
  <behavior>
    - Test: GET /discover without X-Payment header returns 402 with payment requirements JSON including scout wallet, ATA, USDC mint, and price
    - Test: GET /discover with valid X-Payment header returns 200 with { proposals: [...] } (mock ScoutAgent.discoverProposals, mock verifyAndSettlePayment)
    - Test: GET /discover?q=defi+grants passes query parameter to ScoutAgent.discoverProposals
    - Test: POST /evaluate without X-Payment header returns 402 with payment requirements JSON including analyzer wallet, ATA, USDC mint, and price
    - Test: POST /evaluate with valid X-Payment header and proposal body returns 200 with evaluation (mock AnalyzerAgent.evaluateProposal, mock verifyAndSettlePayment)
    - Test: POST /evaluate with invalid proposal body returns 400
  </behavior>
  <action>
    1. Create `src/servers/scout-server.ts`:
       - Export `createScoutServer(options?: { port?: number })` that returns { app: Express, start: () => Promise<Server> }
       - Create Express app with express.json() middleware
       - Instantiate TypedEventBus and ScoutAgent (bus constructor), call scout.initialize()
       - Load scout keypair via getWeb3Keypair('scout')
       - Configure x402Middleware with: recipientWallet = scout keypair publicKey, usdcMint = DEVNET_USDC_MINT, priceUsdc = 1000 (0.001 USDC), connection = getConnection(), cluster = 'devnet'
       - Route: GET /discover with x402Middleware, handler calls scout.discoverProposals(req.query.q as string || 'solana grants'), returns { proposals, txSignature: req.x402Signature }
       - Route: GET /health returns 200 { status: 'ok', agent: 'scout', port }
       - Default port: 4001 (from env SCOUT_PORT or default)
       - The `start` function calls app.listen and returns the Server instance

    2. Create `src/servers/analyzer-server.ts`:
       - Export `createAnalyzerServer(options?: { port?: number })` that returns { app: Express, start: () => Promise<Server> }
       - Same pattern as scout-server but for AnalyzerAgent
       - Load analyzer keypair via getWeb3Keypair('analyzer')
       - priceUsdc = 2000 (0.002 USDC -- evaluation is more expensive than discovery)
       - Route: POST /evaluate with x402Middleware, expects { proposal: Proposal } in body, calls analyzer.evaluateProposal(req.body.proposal), returns { evaluation, txSignature: req.x402Signature }
       - Validate body has proposal field, return 400 if missing
       - Route: GET /health returns 200 { status: 'ok', agent: 'analyzer', port }
       - Default port: 4002 (from env ANALYZER_PORT or default)

    3. Create `src/servers/start-servers.ts`:
       - Simple script that creates and starts both servers
       - Logs port info for each server
       - Handles SIGINT/SIGTERM for graceful shutdown (close both servers)
       - Can be run with: `npx tsx src/servers/start-servers.ts`

    4. Create test files:
       - `tests/unit/scout-server.test.ts`: Import createScoutServer, use supertest-like approach (use app directly with node http, or just call the Express app). Mock getWeb3Keypair, getConnection, ScoutAgent, verifyAndSettlePayment. Test 402 response without header. Test 200 response with mocked valid header. Test query parameter passthrough.
       - `tests/unit/analyzer-server.test.ts`: Same pattern. Mock AnalyzerAgent. Test 402, 200 with valid payment, 400 on missing body.

    Note: For unit tests, mock all Solana operations and agent methods. The servers should be testable without network. Use vi.mock for keys, connection, agents, and x402 verify. The app object can be tested by making HTTP requests to it via a test server (app.listen on port 0 for random port, then fetch against it, then close).

    IMPORTANT: Do NOT modify any existing agent files (scout-agent.ts, analyzer-agent.ts, etc). Servers wrap existing agents without changes.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/scout-server.test.ts tests/unit/analyzer-server.test.ts -x</automated>
  </verify>
  <done>
    - Scout server exposes GET /discover behind x402 middleware on port 4001
    - Analyzer server exposes POST /evaluate behind x402 middleware on port 4002
    - Both return 402 with correct payment requirements when no X-Payment header
    - Both return service results when payment is valid
    - start-servers.ts can launch both servers
    - All server unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for full x402 payment cycle</name>
  <files>tests/integration/x402-payment.test.ts</files>
  <behavior>
    - Test: Full x402 cycle -- start scout server, call /discover without payment (get 402), manually create SPL transfer tx matching requirements, retry with X-Payment header, receive proposals (requires devnet connection and funded wallets)
    - Test: wrapFetchWithPayment against live scout server -- single call auto-handles 402 and returns proposals
    - Test: Payment produces a transaction signature that can be confirmed on devnet
  </behavior>
  <action>
    1. Create `tests/integration/x402-payment.test.ts`:
       - Import createScoutServer from src/servers/scout-server.ts
       - Import wrapFetchWithPayment from src/lib/x402/client.ts
       - Import getWeb3Keypair, getConnection, DEVNET_USDC_MINT

       - Use describe.skipIf pattern (same as other integration tests): skip if keys/governance.json does not exist or if SKIP_DEVNET_TESTS env is set

       - Test "x402 402 response contains valid payment requirements":
         * Start scout server on random port (port 0)
         * Fetch GET /discover without headers
         * Assert status 402
         * Assert body has x402Version, scheme, network, payment with recipientWallet, tokenAccount, mint, amount
         * Close server

       - Test "full x402 payment cycle with wrapFetch":
         * Start scout server on random port
         * Get governance keypair (the "paying" agent)
         * Create wrapped fetch via wrapFetchWithPayment(fetch, { keypair: govKeypair, connection, usdcMint: DEVNET_USDC_MINT, maxPaymentUsdc: 10000 })
         * Call wrapped fetch against scout server /discover endpoint
         * Assert response status 200
         * Assert response body has proposals array
         * Assert response or earlier 402->pay cycle produced a tx (check logs or return value)
         * Close server

       - Test "payment tx signature is valid on devnet":
         * Same flow as above but extract txSignature from response body
         * Call connection.getTransaction(signature) or connection.confirmTransaction
         * Assert transaction exists and is confirmed

       - Mark all devnet tests with reasonable timeout (60s) matching vitest.config.ts

    2. Note: These tests require:
       - Agent keypairs in keys/ directory (from Phase 1 setup)
       - Funded wallets with USDC (from Phase 1 fund-wallets script)
       - Devnet connectivity
       - Tests should skip gracefully if prerequisites are missing (no hard failures)
  </action>
  <verify>
    <automated>pnpm test -- tests/integration/x402-payment.test.ts -x</automated>
  </verify>
  <done>
    - Integration test verifies full x402 cycle: 402 response -> SPL payment -> content delivery
    - At least one test produces a real on-chain USDC transfer signature on devnet
    - Tests skip gracefully when devnet/keys unavailable
    - `pnpm test` (full suite) passes with no regressions
  </done>
</task>

</tasks>

<verification>
- `pnpm test -- tests/unit/scout-server.test.ts tests/unit/analyzer-server.test.ts tests/integration/x402-payment.test.ts -x` -- all server and integration tests pass
- `pnpm exec tsc --noEmit` -- no TypeScript compilation errors
- `pnpm test` -- full test suite passes (no regressions from phases 1-5)
- src/servers/ directory contains scout-server.ts, analyzer-server.ts, start-servers.ts
- No existing agent files were modified
</verification>

<success_criteria>
- Scout and Analyzer each have x402-gated HTTP endpoints
- /discover returns 402 without payment, returns proposals with valid payment
- /evaluate returns 402 without payment, returns evaluation with valid payment
- wrapFetch can automatically pay for and receive service from a peer agent
- At least one on-chain USDC transfer verified on devnet (PAY-02)
- start-servers.ts launches both servers for demo use
</success_criteria>

<output>
After completion, create `.planning/phases/06-x402-agent-payment-economy/06-02-SUMMARY.md`
</output>
